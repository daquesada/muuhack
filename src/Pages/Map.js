import React, { Fragment } from 'react'
import { Map as MapComponent } from '../components/Map';

export const Map = ()=>{
    return(
        <Fragment>
            <MapComponent/>
        </Fragment>
    )
}